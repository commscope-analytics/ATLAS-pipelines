
       filter {
         if [log_type] == "debug" {
           if [log_message] == "Num of connected RU" or [log_message] == "Number of RU currently connected" {
             grok {
               match => {"log_message" => "%{GREEDYDATA:rucc}%{NUMBER:ru_number}"} }
          } 
         }

       }
